import styled from "styled-components";

export const StyleSelect = styled.select`
  font-family: "muli";
  font-size: 1rem;
  padding: 0.5rem 1rem;
  border-radius: 0px;
  border: 1px solid #000;
`;
