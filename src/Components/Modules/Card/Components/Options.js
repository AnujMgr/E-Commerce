import React from "react";
import { 
    StyleOptionOverlay
} from "../CardStyle";

const Options = (props) => {

  	return (
    	<React.Fragment> 
        <StyleOptionOverlay />
    	</React.Fragment>
  	);
};

export default Options;
 