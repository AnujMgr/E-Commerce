import styled from "styled-components";

const StyleSelect = styled.select`
  cursor: pointer;
  opacity: 0;
  width: 100%;
  height: 100%;
  position: absolute;
  left: 0;
  top: 0;
`;

export default StyleSelect;
